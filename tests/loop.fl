$include <std.fl>

function spark(int argc, str[] argv) -> int {
	int idx = 10;
	while idx {
		prints("Hello world!");
		idx--;
	}

	idx = 10;
	loop {
		printi(idx);
		if !idx {
			return 0;
		}
		idx--;
	}
}