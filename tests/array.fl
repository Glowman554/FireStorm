$include <std.fl>

function spark(int argc, str[] argv) -> int {
	str[] test = allocate(8 * 4);
	int[] test2 = allocate(8 * 4);

	int idx = 0;
	while idx < 4 {
		test[idx] = "hi";
		test2[idx] = idx;
		idx = idx + 1;
	}

	idx = 0;
	while idx < 4 {
		prints(test[idx]);
		printi(test2[idx]);
		idx = idx + 1;
	}

	return 0;
}