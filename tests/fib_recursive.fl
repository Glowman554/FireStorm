$include <std.fl>

function spark(int argc, str[] argv) -> int {
	printi(fib(10));
	return 0;
}

function fib(int n) -> int {
    if (n == 0) | (n == 1) {
        return n;
	} else {
    	return fib(n - 1) + fib(n - 2); 
	}
}